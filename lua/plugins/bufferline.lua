return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      numbers = "buffer_id",
      themeable = true, -- sometimes this does what I want, but only when I change it??
      -- indicator = {
      --   style = "underline",
      -- },
      diagnostics = false,
      -- separator_style = "thick",
    },
  },
}
